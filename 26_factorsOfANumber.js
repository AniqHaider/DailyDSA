// taking a number to run testcase on it
const num = 66;

for(let i = 1; i <num; i++) {

    // checking if the number is the factor 
    if(num % i == 0) 
        console.log(i);
 }
 console.log(num);